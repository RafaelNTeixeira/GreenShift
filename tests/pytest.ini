[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Coverage settings
addopts = --cov=../config/custom_components/green_shift --cov-report=html --cov-report=term-missing
# To run without coverage, use: pytest --no-cov
