# ========== SETTINGS TAB ==========
title: Settings
path: settings
icon: mdi:cog
type: panel
cards:
  - type: vertical-stack
    cards:
      - type: markdown
        content: |
          # ⚙️ System Configuration
          Manage your preferences, cost settings and view system health. 🔧

      # --- SYSTEM STATUS (Read-Only) ---
      - type: entities
        title: 📊 System Status
        show_header_toggle: false
        state_color: true
        entities:
          - entity: sensor.research_phase
            name: Current Phase
            icon: mdi:flask-outline
            secondary_info: last-updated
          
          - entity: sensor.energy_baseline
            name: Learned Baseline
            icon: mdi:chart-bell-curve-cumulative

      # --- CONFIGURATION (User Inputs) ---
      - type: entities
        title: 🎛️ Preferences & Costs
        show_header_toggle: false
        entities:
          # --- SECTION: GOALS ---
          - type: section
            label: "Energy Goals"
          
          - type: conditional
            conditions:
              - entity: sensor.research_phase
                state: baseline
            row:
              type: attribute
              entity: sensor.research_phase
              attribute: days_remaining
              name: "🔒 Goals Locked (Calibration)"
              icon: mdi:lock-clock
              suffix: "days left"

          - type: conditional
            conditions:
              - entity: sensor.research_phase
                state: active
            row:
              entity: input_number.energy_saving_target
              name: "Weekly Reduction Target (%)"
              icon: mdi:target

          # --- SECTION: ECONOMICS ---
          - type: section
            label: "Costs & Currency"
          
          - entity: input_select.currency
            name: 💵 Currency
            icon: mdi:cash-100
          
          - entity: input_number.electricity_price
            name: Electricity Price (per kWh)
            icon: mdi:lightning-bolt-circle

      # --- HELP FOOTER ---
      - type: markdown
        content: |
          ### ℹ️ Quick Guide
          * **🔬 Baseline Phase:** The system learns your normal usage for 14 days.
          * **📉 Reduction Target:** Once active, the AI will set weekly challenges to reduce your consumption by this percentage.
          * **💡 Tips:** Adjust settings based on your energy goals and local electricity rates.
